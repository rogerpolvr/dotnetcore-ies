@model Model.Cadastros.Departamento
@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    @{await Html.RenderPartialAsync("_DetailsContent"); }
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.DepartamentoID">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
